rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // All reads and writes go through Cloud Functions (Admin SDK bypasses these rules).
    match /{document=**} {
      allow read: if false;
      allow write: if false;
    }
  }
}
